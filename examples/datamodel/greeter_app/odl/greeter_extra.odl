%define {
    select Greeter {
        /*
            Echos back the data.

            Simple data echo method.

            @param data Some data
            
            @return
            The data
        */
        variant echo(%in %mandatory variant data);
    }
}